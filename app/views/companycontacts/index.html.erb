           <div class="row">   
                    <div class="col-lg-12">
                    
<h1 class="page-header">Контакты компании</h1>
                        <ol class="breadcrumb">
                            <li class="active">
                                <i class="fa fa-dashboard"></i> <%= link_to "Админка", admin_path %>
                            </li>
                            <li class="active">
                                <i class="fa fa-dashboard"></i> Контакты компании
                            </li>
                        </ol>
<div class="row">
                    <div class="col-lg-12">
<div class="table-responsive">      
<table class="table table-bordered table-hover">
  <thead>
    <tr>
      <th>Название</th>
      <th>Slug</th>
      <th>Пункт меню</th>
      <th>meta Description</th>
      <th>meta Keywords</th>
      <th>Картинка</th>
      <th>Контент</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @companycontacts.each do |companycontact| %>
      <tr>
        <td><%= companycontact.title %></td>
        <td><%= companycontact.slug %></td>
        <td><%= companycontact.punktmenu %></td>
        <td><%= companycontact.description %></td>
        <td><%= companycontact.keywords %></td>
        <td><%= image_tag companycontact.image %></td>
        <td><%= companycontact.body %></td>
        <td><%= link_to 'Просмотр', companycontact %></td>
        <td><%= link_to 'Изменить', edit_companycontact_path(companycontact) %></td>
        <td><%= link_to 'Удалить', companycontact, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div></div></div>
<br>

<%= link_to 'Новые контакты компании', new_companycontact_path %>
</div>
                </div>